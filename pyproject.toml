[project]
name = "bayescycle"
version = "0.1.0"
description = "Python workflow CLI for bayeswire model authoring and Bayesite engine runs."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    # The harness and the language move in lockstep: exact-commit pin, bumped
    # deliberately (the pin-bump diff is the compatibility review).
    "bayeswire @ git+https://github.com/StefanSko/bayeswire.git@15cdc51e3e09bed56838248f9b3becd2bbaf8423",
]

[project.optional-dependencies]
inproc = [
    "jaxstanv5 @ git+https://github.com/StefanSko/jaxstanv5.git@597d7758f22f7f2f3fbb791c4deb6c56c47b9eda",
]

[project.scripts]
bayescycle = "bayescycle._cli:main"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.13.0",
    "ty==0.0.49",
]

[build-system]
requires = ["uv_build>=0.8.0,<0.9.0"]
build-backend = "uv_build"

[tool.ruff]
line-length = 100
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ty.environment]
python = "./.venv"
python-version = "3.12"
root = ["./src", "./tests"]
