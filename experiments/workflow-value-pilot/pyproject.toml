[project]
name = "workflow-value-pilot"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = [
    "bayescycle[inproc]==0.7.0",
    "jax==0.10.2",
    "blackjax==1.5",
    "numpyro",
    "arviz==0.22.0",
    "matplotlib",
]

[tool.uv.workspace]
members = []

[tool.uv.sources]
bayeswire = { path = "../../packages/bayeswire", editable = true }
bayesjax = { path = "../../packages/bayesjax", editable = true }
bayescycle = { path = "../../packages/bayescycle", editable = true }
